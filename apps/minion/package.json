{
  "name": "minion",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "env": "workspace:*",
    "geoapify": "workspace:*",
    "injectx": "^1.0.4",
    "ping": "workspace:*",
    "running": "workspace:*",
    "strava": "workspace:*",
    "users": "workspace:*",
    "workflow": "workspace:*"
  }
}
